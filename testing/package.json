{
  "name": "cypress_drupal_testing",
  "scripts": {
    "test:headless": "cypress run --headless --browser chrome",
    "test:head": "cypress run --browser chrome",
    "open": "cypress open",
    "vr:test": "as-a applitools npx cypress run --headless --browser chrome --spec \"cypress/integration/vr_recipe_listing_pages.spec.js\""
  },
  "devDependencies": {
    "@applitools/eyes-cypress": "latest",
    "as-a": "^1.3.1",
    "cypress": "^8.1.0"
  }
}
